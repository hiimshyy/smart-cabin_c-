# Build artifacts
build/
*.o
*.d

# Downloaded packages / local dep extracts (dev machine, no-sudo sqlite)
*.deb

# Test binaries built by tests/run_tests.sh (output goes to /tmp)
tests/*.out

# Compiled binaries (root-level)
/face_recog_app
/enroll_faces
/capture_person
/add_person
/probe_models
/migrate_fdb

# Runtime data (mỗi user có dataset riêng)
faces/
db/*.fdb
# SQLite resident DB + WAL/SHM sidecars (runtime data, per-deployment)
db/*.db
db/*.db-wal
db/*.db-shm

# Editor / OS junk
.vscode/
.idea/
.DS_Store
*.swp
*~

# Kiro CLI resume markers / session junk
chat
legacy/bug
legacy/camera

# YOLO person-detection models (not part of face_recog_app)
model/person_det/

# Legacy vendor binary is tracked, but not build outputs
!legacy/retinaface_demo_a733
