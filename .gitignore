# Build artifacts
build/
*.o
*.d

# Compiled binaries (root-level)
/face_recog_app
/enroll_faces
/capture_person
/add_person
/probe_models

# Runtime data (mỗi user có dataset riêng)
faces/
db/*.fdb

# Editor / OS junk
.vscode/
.idea/
.DS_Store
*.swp
*~

# Kiro CLI resume markers / session junk
chat
legacy/bug
legacy/camera

# YOLO person-detection models (not part of face_recog_app)
model/person_det/

# Legacy vendor binary is tracked, but not build outputs
!legacy/retinaface_demo_a733
